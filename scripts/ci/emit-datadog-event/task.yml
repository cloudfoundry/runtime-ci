---
platform: linux

image_resource:
- type: docker
  repository: docker:///relintdockerhubpushbot/emit-datadog-event

inputs:
- name: runtime-ci

run:
  path: runtime-ci/scripts/ci/emit-datadog-event/task

params:
  TITLE:
  TEXT:
  PRIORITY:
  AWS_ENV:
  DATADOG_API_KEY:
