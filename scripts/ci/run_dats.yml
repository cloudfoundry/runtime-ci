---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
  - name: deployments-runtime

params:
  BOSH_USER: ~
  BOSH_PASSWORD: ~
  BOSH_TARGET: ~
  ACCEPTANCE_TESTS_MANIFEST: ~

run:
  path: deployments-runtime/runtime-ci/scripts/ci/run_dats
