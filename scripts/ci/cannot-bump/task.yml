---
platform: linux

image_resource:
  type: docker
  source:
    repository: docker:///relintdockerhubpushbot/cannot-bump

inputs:
- name: runtime-ci

run:
  path: runtime-ci/scripts/ci/cannot-bump/task

params:
  URL:
  TOKEN:
  REASON:
