---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/extract-security-groups-from-bbl-stack

inputs:
- name: runtime-ci
- name: env-repo

outputs:
- name: security-groups

run:
  path: runtime-ci/scripts/ci/extract-security-groups-from-bbl-stack/task

params:
  STATE_DIR_PATH:
  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
