---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/update-gcp-cloud-config

inputs:
- name: runtime-ci
- name: terraform
- name: env-repo
- name: cf-gcp-infrastructure

outputs:
- name: updated-env-repo

run:
  path: runtime-ci/scripts/ci/update-gcp-cloud-config/task

params:
  NATS_PASSWORD:
  MBUS_PASSWORD:
  HM_PASSWORD:
  BLOBSTORE_AGENT_PASSWORD:
  BLOBSTORE_DIRECTOR_PASSWORD:
  POSTGRES_PASSWORD:
