---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
  - name: cf-release
  - name: deployments-runtime

outputs:
  - name: manifest

run:
  path: deployments-runtime/runtime-ci/scripts/ci/create_cf_release_manifest

params:
  INFRASTRUCTURE:
  ENVIRONMENT:
  OUTPUT_DIR: manifest
