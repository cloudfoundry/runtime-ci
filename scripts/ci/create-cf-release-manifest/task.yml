---
platform: linux

image: docker:///relintdockerhubpushbot/create-cf-release-manifest

inputs:
- name: cf-release
- name: runtime-ci
- name: deployments

outputs:
- name: manifest

run:
  path: runtime-ci/scripts/ci/create-cf-release-manifest/task

params:
  INFRASTRUCTURE:
  STUBS_PATH: deployments/stubs/cf/*.yml
  OUTPUT_DIR: manifest
