---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
  - name: deployments-runtime

params:
  BOSH_USER: ~
  BOSH_PASSWORD: ~
  BOSH_TARGET: ~
  SMOKE_TESTS_MANIFEST: ~
  DEPLOYMENT_TAG: ~
  DEPLOYMENTS_DIR: ~
  DATADOG_API_KEY: ~

run:
  path: deployments-runtime/runtime-ci/scripts/ci/run_diego_smoke_tests
