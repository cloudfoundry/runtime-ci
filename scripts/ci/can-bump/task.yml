---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/can-bump

inputs:
- name: runtime-ci

run:
  path: runtime-ci/scripts/ci/can-bump/task

params:
  URL:
  TOKEN:
