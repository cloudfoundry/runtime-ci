---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
- name: runtime-ci

run:
  path: runtime-ci/scripts/ci/emit_datadog_metric/task

params:
  METRIC:
  TAGS:
  DATADOG_API_KEY:
  VALUE:
