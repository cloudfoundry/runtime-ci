---
platform: linux

image: docker:///relintdockerhubpushbot/create-release

inputs:
- name: cf-release-develop
- name: runtime-ci

outputs:
- name: cf-release-out

run:
  path: runtime-ci/scripts/ci/create-release/task

params:
  CF_RELEASE_DIR:
  TARBALL:
  OUTPUT_DIR: cf-release-out
