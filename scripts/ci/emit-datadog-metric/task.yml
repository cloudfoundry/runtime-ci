---
platform: linux

image: docker:///relintdockerhubpushbot/emit-datadog-metric

inputs:
- name: runtime-ci

run:
  path: runtime-ci/scripts/ci/emit-datadog-metric/task

params:
  METRIC:
  TAGS:
  DATADOG_API_KEY:
  VALUE:
