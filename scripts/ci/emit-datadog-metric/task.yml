---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: docker:///relintdockerhubpushbot/emit-datadog-metric

inputs:
- name: runtime-ci

run:
  path: runtime-ci/scripts/ci/emit-datadog-metric/task

params:
  METRIC:
  TAGS:
  DATADOG_API_KEY:
  VALUE:
