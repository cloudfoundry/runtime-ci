---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
  - name: deployments-runtime

run:
  path: deployments-runtime/runtime-ci/scripts/ci/cannot_bump

params:
  URL:
  TOKEN:
  REASON:
