---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/write-creds

inputs:
- name: runtime-ci

outputs:
- name: bbl-state

run:
  path: runtime-ci/scripts/ci/write-creds/task

params:
  BBL_STATE_DIR:
  DIRECTOR_ADDRESS:
  DIRECTOR_USERNAME:
  DIRECTOR_PASSWORD:
  DIRECTOR_CA_CERT:
