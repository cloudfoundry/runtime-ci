---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
- name: runtime-ci

outputs:
  - name: logs-dir

run:
  path: runtime-ci/scripts/ci/run-errand/task

params:
  BOSH_DIRECTOR:
  BOSH_USER:
  BOSH_PASSWORD:
  DEPLOYMENT_NAME:
  ERRAND_NAME:
  LOGS_DIR: logs-dir
