---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
  - name: cf-release-develop
  - name: deployments-runtime
  - name: version

outputs:
  - name: cf-release-out

run:
  path: deployments-runtime/runtime-ci/scripts/ci/create_release

params:
  CF_RELEASE_DIR:
  TARBALL:
  OUTPUT_DIR: cf-release-out
