---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
- name: runtime-ci

run:
  path: runtime-ci/scripts/ci/delete-deployment/task

params:
  BOSH_DIRECTOR:
  BOSH_USER:
  BOSH_PASSWORD:
  DEPLOYMENT_NAME:
