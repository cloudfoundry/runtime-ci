---
platform: linux

image_resource:
  type: docker
  source:
    repository: docker:///relintdockerhubpushbot/deploy-cf-alone

inputs:
- name: runtime-ci
- name: cf-deployment
- name: cf-release
- name: etcd-release
- name: consul-release
- name: private-credentials
- name: stemcell

run:
  path: runtime-ci/scripts/ci/deploy-cf-alone/task

params:
  STUB_PATH:
  STUB_PARTS_PATH:
  BOSH_DIRECTOR:
  BOSH_USER:
  BOSH_PASSWORD:
