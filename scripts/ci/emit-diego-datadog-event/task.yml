---
platform: linux

image: docker:///cloudfoundry/runtime-ci

params:
  DATADOG_API_KEY:
  STATE:
  ENVIRONMENT:
  DEPLOYMENT:

inputs:
  - name: runtime-ci

run:
  path: sh
  args:
    - -c
    - |
      runtime-ci/scripts/ci/emit-diego-datadog-event/task $STATE $ENVIRONMENT $DEPLOYMENT
