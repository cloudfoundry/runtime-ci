---
platform: linux

image_resource:
  type: docker
  source:
    repository: docker:///relintdockerhubpushbot/setup-infrastructure

inputs:
- name: runtime-ci
- name: env-repo

outputs:
- name: updated-env-repo

run:
  path: runtime-ci/scripts/ci/setup-infrastructure/task

params:
  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
