---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
- name: runtime-ci

run:
  path: runtime-ci/scripts/ci/emit_datadog_event

params:
  TITLE:
  TEXT:
  PRIORITY:
  AWS_ENV:
  DATADOG_API_KEY:
