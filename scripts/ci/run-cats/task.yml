---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/run-cats

inputs:
- name: runtime-ci
- name: cf-acceptance-tests

run:
  path: runtime-ci/scripts/ci/run-cats/task

params:
  BACKEND:
  CF_ADMIN_PASSWORD:
  CF_ADMIN_USER:

  INCLUDE_APPS:
  INCLUDE_BACKEND_COMPATIBILITY:
  INCLUDE_DETECT:
  INCLUDE_DIEGO_DOCKER:
  INCLUDE_DIEGO_SSH:
  INCLUDE_INTERNET_DEPENDENT:
  INCLUDE_PRIVILEGED_CONTAINER_SUPPORT:
  INCLUDE_ROUTE_SERVICES:
  INCLUDE_ROUTING:
  INCLUDE_SECURITY_GROUPS:
  INCLUDE_SERVICES:
  INCLUDE_TASKS:
  INCLUDE_V3:

  BINARY_BUILDPACK_NAME:
  GO_BUILDPACK_NAME:
  JAVA_BUILDPACK_NAME:
  NODEJS_BUILDPACK_NAME:
  PHP_BUILDPACK_NAME:
  PYTHON_BUILDPACK_NAME:
  RUBY_BUILDPACK_NAME:
  STATICFILE_BUILDPACK_NAME:

  BROKER_START_TIMEOUT_IN_SECONDS:
  CF_API:
  CF_APPS_DOMAIN:
  CF_PUSH_TIMEOUT_IN_SECONDS:
  DEFAULT_TIMEOUT_IN_SECONDS:
  ASYNC_SERVICE_OPERATION_TIMEOUT_IN_SECONDS:
  EXISTING_USER:
  EXISTING_USER_PASSWORD:
  LONG_CURL_TIMEOUT_IN_SECONDS:
  NODES:
  PERSISTENT_APP_HOST:
  PERSISTENT_APP_ORG:
  PERSISTENT_APP_QUOTA_NAME:
  PERSISTENT_APP_SPACE:
  SKIP_SSL_VALIDATION:
  INCLUDE_SSO:
  USE_HTTP:
