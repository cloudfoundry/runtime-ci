---
platform: linux

image: docker:///relintdockerhubpushbot/run-errand-bbl

inputs:
- name: runtime-ci

outputs:
  - name: logs-dir

run:
  path: runtime-ci/scripts/ci/run-errand-bbl/task

params:
  BOSH_DIRECTOR_FILE:
  BOSH_USER_FILE:
  BOSH_PASSWORD_FILE:
  DEPLOYMENT_NAME:
  ERRAND_NAME:
  LOGS_DIR: logs-dir
